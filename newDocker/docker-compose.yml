version: "3.8"
services:
  nginx:
    container_name: "nginx"
    build: ./requeriments/nginx
    ports:
      - "8080:80"
    volumes:
      - ./html:/var/www/html
    networks:
      - webnet

networks:
  webnet:
